# {{NAME}} — Claude Code Project Instructions

> This project is a **Nissth consumer**. The framework reference below is copied verbatim from the Nissth checkout at `{{FRAMEWORK_ROOT}}` ({{WIRING_SENTENCE}}; see §11.15); §1–§8 and §11 are the framework rules and are not customised here. Project-specific facts live in `ImplementationPlans/SRS.md`, `ImplementationPlans/SDD.md`, `DBL/**`, and `AgentReports/StatusUpdate.md`.
>
> **Status:** Bootstrapped {{DATE}} by `nissth-init` (CLAUDE.md §9.1 step 2). {{STACK_SENTENCE}} No source has been touched by the bootstrap. Next: `ImplementationPlans/SRS.md` + `SDD.md` (§9) if absent, then `Phase_00_DBL_Bootstrap.md`. Framework defects discovered while building this project are reported back to the Nissth repo, not patched here.
