
# ----- Expo / React Native (CLAUDE.md §8.2.5 #2, #10) -----
node_modules/
.expo/
.expo-shared/
dist/
web-build/
coverage/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
# native projects are generated by `expo prebuild` / EAS; the managed workflow keeps them out of git
ios/
android/
# EAS credentials and local state
.eas/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
# secrets never enter the repo
.env
.env.*
