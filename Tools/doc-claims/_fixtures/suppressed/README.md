# Fixture — inline suppression
<!-- doc-claims:allow stale-binding-status — quoting a historical defect -->
Once upon a time this file called the Postgres binding "queued".
<!-- doc-claims:allow fictional-tool — quoting the defect by name -->
The Postgres binding once claimed it ships `query_plan` and `index_drift`.
